import {PresentationLayout} from "@/app/create/components/PresentationLayout";

export default function GeneratedSummaryPage({params}: {
    params: { id: string };
}) {
    return (
        <PresentationLayout params={{ slides: 8, lang: "Русский" }}>
            {/* Список слайдов */}

            <div>
                hello
            </div>
        </PresentationLayout>
    );
};
